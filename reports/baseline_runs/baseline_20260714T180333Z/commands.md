# Baseline commands

Executed from Git Bash:

```bash
PYTHONUTF8=1 .venv/Scripts/python.exe scripts/run_baseline_stage.py --device cuda --run-id baseline_20260714T180333Z
```

Full A (not executed; requires separate approval):

```bash
python scripts/train.py --device cuda --confirm-full-run
```
